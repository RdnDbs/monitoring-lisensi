RewriteEngine On

# Jangan rewrite kalau file atau folder beneran ada
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Rewrite URL lain ke index.php
RewriteRule ^ index.php [L]
